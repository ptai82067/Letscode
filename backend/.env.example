# Database Configuration
# OPTION 1: For managed services (Neon, Render, etc.), use DATABASE_URL:
# DATABASE_URL=postgres://username:password@host:port/dbname?sslmode=require

# OPTION 2: For local development with Docker Compose (RECOMMENDED):
# The docker-compose.yml exposes PostgreSQL on 127.0.0.1:5433
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=courseai_db
DB_SSLMODE=disable

# JWT Configuration
# IMPORTANT: ALWAYS change JWT_SECRET before deploying to production
# Generate a strong secret using: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_IN_PRODUCTION
JWT_EXPIRE_HOURS=24

# Server Configuration
PORT=8080
FRONTEND_URL=http://localhost:5173

# Deployment Environment (set to "production" in production)
# ENV=production

# For Vercel deployments, the frontend will be automatically allowed via VERCEL_URL
# No additional configuration needed
